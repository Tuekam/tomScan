name: tomscan
description: TomScan - Détection des maladies de la tomate
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.2
  geolocator: ^14.0.2
  camera: any
  dio: ^5.9.2
  flutter_map: any
  latlong2: any
  flutter_map_marker_cluster: any
  permission_handler: ^12.0.3
  provider: any
  shared_preferences: any
  fl_chart: any
  flutter_markdown: any 
  http: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
  assets:
      - assets/images/references/early_blight/
      - assets/images/references/healthy/
      - assets/images/references/late_blight/
      - assets/images/references/yellow_curl/
      - assets/images/references/mold/
      - assets/images/references/septoria_spot/